import React from 'react'
import { Link } from 'react-router'

const ShowTimeList = () => (
  <div>
    <h1>Select Show Time</h1>
    <Link to="/">Back to Show Selection</Link>
  </div>
)

export default ShowTimeList